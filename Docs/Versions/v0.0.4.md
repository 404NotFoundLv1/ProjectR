---
title: "ProjectR v0.0.4 任务合同"
subtitle: "建立自动化构建脚本和日志目录"
version: "v1.0"
date: "2026-07-10"
---

# v0.0.4 建立自动化构建脚本和日志目录
**状态**：Completed
**目标**：为长期开发准备可重复编译、打包和 QA 记录。

## 前置状态
- v0.0.3 已完成。

## 本次范围
- 创建 `BuildScripts/BuildEditor.bat`、`BuildScripts/PackageDev.bat`、`BuildScripts/CleanGenerated.bat` 占位。
- 创建 `Docs/Workflow/BuildGuide.md`，写明如何生成工程、编译、打 Development/Shipping。
- 创建 `Saved/AutomationReports` 忽略规则说明。
- 将常见打包地图写入 DefaultGame 或项目设置说明。

## Non-goals
- 不实现下一公共版本。
- 不做无关重构。
- 不静默破坏已记录接口、Tag、Save 字段或资产路径。

## Allowed paths
- `BuildScripts/**`
- `.gitignore`
- `Docs/**`
- `Config packaging map entries only`

## Forbidden paths/actions
- gameplay code
- Content asset creation unrelated to build smoke
- destructive clean commands without path checks

## 跨版本关系
- Depends on: v0.0.3。
- Produces: 为长期开发准备可重复编译、打包和 QA 记录。
- Consumed by: v0.0.5。
- Extension points: 配置、接口骨架、验证脚本、MCP Authoring 入口。
- Must remain compatible: 工程名、模块名、目录、构建入口、GameplayTag 命名。

## 本版本冻结合同
- BuildEditor/PackageDevelopment/Automation entry points
- actual-result reporting

## Codex C++/文件工作
- 先检查真实状态和依赖，再给准确 diff 计划。
- 使用 apply_patch 修改文本文件。
- 执行适用 Build/静态校验并报告实际结果。
- 不把未运行写成通过。

## MCP Operation Manifest
- Run automation/log checks if AutomationTestToolset is enabled; no gameplay asset writes.
- 写入前列出准确 Package/Tool/顺序/停止点；写入后分别报告保存、编译、重启加载和 PIE。

## Human Judgment / Credential
- Approve local UE installation path if auto-discovery finds multiple installations.
- 若需要人工，必须引用 ManualOperationsRunbook 的精确步骤；用户不负责手动编码或重复资产配置。

## 自动验证
- 执行 BuildEditor 或手动编译。
- 检查保护路径、公共合同、KnownIssues 和 Future Compatibility Review。

## 2026-07-11 实施记录（Completed）

### 已创建/修改的合同表面

- 公共入口：`BuildEditor.bat`、`PackageDevelopment.bat`、`CleanGenerated.bat`、`AutomationReport.bat`；`PackageDev.bat` 为零逻辑兼容转发器，共同实现位于 `ProjectRAutomation.ps1`。
- 文档入口：`Docs/Workflow/BuildGuide.md`；完整参数、Engine 发现、退出码、报告 Schema、清理保护和人工例外以该文件为准。
- 打包事实源：`Config/DefaultGame.ini` 恰好列出五张 v0.0.3 冻结地图；脚本读取并验证 Config 后生成 UAT `-MapsToCook`，不维护第二份地图列表。
- 本地证据：`Saved/AutomationReports/<RunId>/<EntryPoint>-<Configuration>/` 下的 `command.txt`、`run.log` 和 SchemaVersion 1 `result.json`；目录由 `/Saved/` 忽略且不进入 CleanGenerated 候选。
- UAT 生成的 `Build/**/FileOpenOrder/` 由精确规则忽略；其他 `Build/` 内容保持可跟踪，避免屏蔽未来平台图标或构建配置。
- 公共脚本最终合同回归与独立复审均实际退出 0，Spec compliance PASS、Code quality Approved；真实 BuildEditor 与 Development Package 及其产物后置条件均已通过，ADR-013 已接受。

### 实际结果矩阵

| 项目 | 实际证据 | 状态 |
|---|---|---|
| BuildEditor Development WhatIf | `v004-final2-build-19f4d9279e6` | `NOT_RUN`；命令已生成，未调用 Build.bat |
| PackageDevelopment Development WhatIf | `v004-final2-pkgdev-19f4d9279e6` | `NOT_RUN`；五张地图已验证，未调用 RunUAT.bat |
| PackageDevelopment Shipping WhatIf | `v004-final2-pkgship-19f4d9279e6` | `NOT_RUN`；兼容入口命令已生成，未调用 RunUAT.bat |
| CleanGenerated Standard Preview | `v004-final2-cleanstd-19f4d9279e6` | PASS；Apply `NOT RUN`，删除 0 |
| CleanGenerated Deep Preview | `v004-final2-cleandeep-19f4d9279e6` | PASS；Apply `NOT RUN`，删除 0 |
| AutomationReport Schema 冒烟 | `v004-final2-report-19f4d9279e6` | PASS |
| 公共脚本最终合同回归 | `Saved/AutomationTests/ProjectRAutomation.Contract.Tests.ps1` | PASS；主线程与独立复审均退出 0，Code quality Approved |
| 真实 ProjectREditor Development Build | `v004-final-build-19f4ed6c55e` | PASS；包装器/子进程退出 0，UBT Succeeded，目标 DLL 已验证 |
| 真实 Development Package/产物核对 | `v004-actual-package-19f4ece2a19` | PASS；UAT/包装器退出 0，五地图及 EXE/Pak/UTOC/UCAS 已验证 |
| Shipping Package | 无 | `NOT RUN` |
| CleanGenerated Apply | 无 | `NOT RUN` |
| Project files generation | 无 | `NOT RUN` |
| Editor restart / PIE / MCP 写入 | 无 | `NOT RUN`；不属于本版本资产范围 |

### 最终状态与下游门

- ProjectR Editor 已正常关闭且没有保存提示；Package 和最终 Build 结束后 Live Coding/UBT/UAT 与 8000 监听均为 0。后续若重新打开，任何 Build/Package 前仍按 ManualOperationsRunbook 第 10 节处理，不强杀或擅自保存。
- 系统卷在真实 Package 前为 12.47 GiB、完成后为 12.10 GiB；`v004-actual-package-19f4ece2a19` 的四个磁盘门均 PASS。
- v0.0.5 草案的 World Partition External Package Allowed paths 与“Save All 后重启”冲突已登记 KI-011/E-012/E-013；只记录下游审查，不修改 v0.0.5 任务页或执行 MCP 写入。
- v0.0.4 全部自动门已通过；`CURRENT_VERSION.md` 按合同保持不修改，版本推进和 Git commit/push 仍需后续单独授权。

## 验收标准
- 脚本文档存在。
- 至少能手动执行编译流程。
- 团队成员能照文档跑。
- 下游版本能通过记录的扩展点继续实现，无需大规模改写本版本。

## 回滚条件
- Build/Editor/PIE 出现当前版本引入的阻断。
- 需要未批准的保护路径或破坏性资产操作。
- 当前实现要求提前创建未来系统。
- Blueprint/Save/Tag/资产路径出现未记录兼容破坏。

## 完成报告
按 AGENTS.md 的 11 项格式输出，并更新 DevelopmentLog、KnownIssues 和 ArchitectureDecisions。

**建议提交名**：`v0.0.4 Add build scripts and build guide`
