---
title: "ProjectR v0.0.3 任务合同"
subtitle: "创建核心地图与 Game Framework"
version: "v1.0"
date: "2026-07-10"
---

# v0.0.3 创建核心地图与 Game Framework
**状态**：Completed

**目标**：建立主菜单、现实据点、网络关卡、测试场的基本流转。

## 前置状态
- v0.0.2 已完成。

## 本次范围
- 创建核心类：GameInstance、GameModeBase、RealityGameMode、NetworkRunGameMode、PlayerController、PlayerState、PlayerCharacter。
- 创建地图：L_MainMenu、L_RealityHub、L_Network_Prototype、L_CombatGym、L_BossGym。
- 设置地图默认 GameMode。
- 创建基础 OpenLevel 流程。

## Non-goals
- 不实现下一公共版本。
- 不做无关重构。
- 不静默破坏已记录接口、Tag、Save 字段或资产路径。

## Allowed paths
- `Source/ProjectR/Core/**`
- `Source/ProjectR/Characters/**`
- `Config/DefaultEngine.ini`
- `Content/ProjectR/Maps/**`
- `Content/ProjectR/Blueprints/**`
- `Content/__ExternalActors__/ProjectR/Maps/L_MainMenu/**`
- `Content/__ExternalActors__/ProjectR/Maps/L_RealityHub/**`
- `Content/__ExternalActors__/ProjectR/Maps/L_Network_Prototype/**`
- `Content/__ExternalActors__/ProjectR/Maps/L_CombatGym/**`
- `Content/__ExternalActors__/ProjectR/Maps/L_BossGym/**`
- `Content/__ExternalObjects__/ProjectR/Maps/L_MainMenu/**`
- `Content/__ExternalObjects__/ProjectR/Maps/L_RealityHub/**`
- `Content/__ExternalObjects__/ProjectR/Maps/L_Network_Prototype/**`
- `Content/__ExternalObjects__/ProjectR/Maps/L_CombatGym/**`
- `Content/__ExternalObjects__/ProjectR/Maps/L_BossGym/**`
- `Docs/**`

## Forbidden paths/actions
- Deleting template Variant
- GAS/combat/companion/director business logic
- unapproved asset moves

## 跨版本关系
- Depends on: v0.0.2。
- Produces: 建立主菜单、现实据点、网络关卡、测试场的基本流转。
- Consumed by: v0.0.4。
- Extension points: 配置、接口骨架、验证脚本、MCP Authoring 入口。
- Must remain compatible: 工程名、模块名、目录、构建入口、GameplayTag 命名。

## 本版本冻结合同
- GameInstance/GameMode/PlayerController/PlayerState/PlayerCharacter lifecycle
- formal map IDs and travel entry

## Codex C++/文件工作
- 先检查真实状态和依赖，再给准确 diff 计划。
- 使用 apply_patch 修改文本文件。
- 执行适用 Build/静态校验并报告实际结果。
- 不把未运行写成通过。

## MCP Operation Manifest
- Duplicate approved template map, create thin Blueprint subclasses, set parents/CDO/GameMode, place spawn actors, save/compile/load/PIE.
- 写入前列出准确 Package/Tool/顺序/停止点；写入后分别报告保存、编译、重启加载和 PIE。

## Human Judgment / Credential
- Judge camera composition and 2.5D framing; provide PASS/FAIL and screenshots.
- 若需要人工，必须引用 ManualOperationsRunbook 的精确步骤；用户不负责手动编码或重复资产配置。

## 自动验证
- 逐张地图 PIE。
- OpenLevel 测试。
- 检查保护路径、公共合同、KnownIssues 和 Future Compatibility Review。

## 验收标准
- 所有地图可打开。
- 玩家在每张测试地图正常出生。
- 能从主菜单进入现实据点。
- 下游版本能通过记录的扩展点继续实现，无需大规模改写本版本。

## 回滚条件
- Build/Editor/PIE 出现当前版本引入的阻断。
- 需要未批准的保护路径或破坏性资产操作。
- 当前实现要求提前创建未来系统。
- Blueprint/Save/Tag/资产路径出现未记录兼容破坏。

## 完成报告
按 AGENTS.md 的 11 项格式输出，并更新 DevelopmentLog、KnownIssues 和 ArchitectureDecisions。

## 实施与验收结果

- 创建 15 个 C++ 文件，建立 `EPRMapId`、`UPRGameInstance::OpenMap` 和六个正式 APR Framework 基类；Source 总数为 106。
- 创建五张正式 World Partition 地图和八个 Blueprint；新增主 Package 13、External Actor 326、External Object 10，新增 UE Package 合计 349，工程 Package 总数为 1102。
- 八个 Blueprint 均以 warnings-as-errors 编译并精确保存；重启后父类、CDO、固定相机、地图 Override、PlayerStart、依赖和 Dirty 状态验证通过。
- 正式 Build 初次完成 11/11 actions；最终 Build 报告 Target up to date。两次均退出 0 且 `Result: Succeeded`。
- MainMenu 自动 PIE 在 1 秒后通过枚举式入口进入 RealityHub；四张玩法地图均生成正式 Pawn、产生 Framework spawn 日志、返回非空截图数据且无新增阻断日志。
- 用户确认 RealityHub、Network Prototype、CombatGym、BossGym 的固定侧视相机构图全部 PASS。
- 现有 753 个 UE Package、uproject、Build.cs、Targets、DefaultGameplayTags 和模板/Variant 文件未修改；53 个 GameplayTags 保持不变。
- 未暂存、提交、推送、运行 GC 或推进 `CURRENT_VERSION.md`。

## Future Compatibility Review

- v0.0.4 可直接消费五个稳定地图路径、`EPRMapId`、`OpenMap`、默认启动配置和既有 `ProjectREditor` 构建入口，不需要改写本版本 Framework。
- v0.1.0 可在 `APRPlayerCharacter` 上增量实现 Enhanced Input、移动和 2.5D 平面约束，无需重新设置 Blueprint 父类。
- 正式 Framework 不依赖模板 `AProjectR*`、Variant、GAS、Save、Director、Steam 或未来内容类型。
- 未新增 Save 字段、GameplayTag、未来玩法 API 或 packaging map list；LFS 只影响 Package 存储方式，不改变 UE 引用路径。

**建议提交名**：`v0.0.3 Create core framework and formal maps`
