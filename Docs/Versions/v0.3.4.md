---
title: "v0.3.4 - 濒死保护与分歧缓存区完整化"
version: "v0.3.4"
status: "Completed"
date: "2026-07-24"
---

# 目标

在一个完整版本中交付每轮一次的濒死分歧：主同步 Companion 在玩家正式死亡后提供“挽救、直接离开、直面挑战”三项有限选择。系统以本地确定性方式复用既有 Combat、Relationship、Dialogue 与 Save 合同，并发布供后续版本消费的值型结果。

# 范围与冻结边界

- 只在玩家正式 `Combat.Event.Death` 后处理；伤害链、死亡顺序和复活继续唯一经 `UPRCombatSubsystem`。
- 资格必须同时满足：Authority、当前 Player Avatar 已 Dead、有效 Primary、已加载 Profile、`Trust >= 50`、`Overload < 80`、本轮尚未消费。
- P0 的“每轮”固定为当前 GameInstance/Profile 运行会话：普通地图旅行不重置；成功 Create/Load Profile 才开始新会话。v0.4.0 的正式 RunState 只替换此重置信号，不改变结果 Schema。
- 不实现 PlayerProfile、Director、Room、撤离、资源保留、奖励、账号删除、墓园、技能树、网络、LLM、TTS、自由文本或自动复活。
- 不修改 GameplayTag、Input、Config、Save Schema、地图、Combat/QTE/Companion/Boss/CombatHUD 核心逻辑；不把 ReverseBurn QTE 或 Companion Support 升级为 Revive。
- RuntimeState、Widget、Timer、Delegate、Actor、ASC、Effect/Spec Handle 和弱 UObject 引用不得进入 Save。

# 结果、状态机与选择

新增 `EPRDivergenceState`（Idle、AwaitingChoice、Resolving、Completed）、`EPRDivergenceChoice`（None、Rescue、Leave、FaceChallenge）、`EPRDivergenceResolution`（Applied、RejectedIneligible、RejectedAlreadyConsumed、RejectedCombat、RejectedInvalid、Expired、Cancelled）与 `EPRDivergenceFutureDisposition`（None、RescueEvacuationRequested、LeaveRunRequested、ChallengeContinues）。

新增值型 `FPRDivergenceRuntimeState`、`FPRDivergenceResult` 和 Result/State Native Delegate。结果必须含 Request/Result/DeathEvent ID、Companion、Choice、Resolution、FutureDisposition、Revive 状态与比例、关系前后快照、Delta、SaveRequestId、最近有效同 Primary Dialogue Result/Choice ID 和 WorldTime；不含 UObject。

```text
Idle -> AwaitingChoice -> Resolving -> Completed
AwaitingChoice -> Expired / Cancelled -> Completed
```

- 活动项创建时立即消费本轮资格；重复 DeathEvent、第二次死亡、超时、旅行、重选、Pawn replacement、World Cleanup、PIE Stop 与 Deinitialize 均不得返还。
- 选择窗口固定 30 秒。超时保持 Dead，不应用 Delta、不请求保存。
- `E / Input.Interact` 选择 Rescue；`R / Input.QTE.Reject` 选择 Leave；`F / Input.Execute` 选择 FaceChallenge。不得新增 InputAction 或 GameplayTag。
- Rescue：`FPRReviveRequest` 固定 `HealthFraction=0.25`、`ShieldFraction=0`，FutureDisposition 为 `RescueEvacuationRequested`。
- Leave：不调用 Revive，保持 Dead，FutureDisposition 为 `LeaveRunRequested`。
- FaceChallenge：`HealthFraction=0.10`、`ShieldFraction=0`，FutureDisposition 为 `ChallengeContinues`。
- Rescue/FaceChallenge 只有 Revive 实际 Applied 后才请求关系 Delta；Revive 失败则不写关系、不保存。Leave 直接请求 Delta。
- 仅实际关系变化才调用一次 `RequestSaveCurrentProfile`。

固定关系 Delta（Trust/Affection/Evaluation/Overload）：

| Primary | Rescue | Leave | FaceChallenge |
|---|---:|---:|---:|
| Axiom | +2/+1/+1/+20 | -1/-1/+1/0 | 0/0/+2/0 |
| Kindle | +2/+2/0/+25 | -2/-1/0/0 | +2/+1/+2/0 |
| Null | +1/+2/+1/+20 | -1/-2/+1/0 | +1/+1/+2/0 |

固定主提示分别为：

- Axiom：“存续概率仍可修正。接受挽救、主动离开，或继续证明。”
- Kindle：“还没结束！让我拉你回来、现在撤，还是带着这点血继续冲？”
- Null：“删除流程卡住了。让我救、自己走，还是拿这点命继续赌？”

系统仅订阅值型 `FPRCombatEvent`、`FPRDialogueResult`、Primary Sync 和 Save Operation；Dialogue Result 只作为结果上下文，不读取 Dialogue 私有队列，也不改变资格或伤害。

# C++、工具与 Package Manifest

Create：

```text
Source/ProjectR/Divergence/PRDivergenceTypes.h
Source/ProjectR/Divergence/PRDivergenceTypes.cpp
Source/ProjectR/Divergence/PRDivergenceDataAsset.h
Source/ProjectR/Divergence/PRDivergenceDataAsset.cpp
Source/ProjectR/Divergence/PRDivergenceSubsystem.h
Source/ProjectR/Divergence/PRDivergenceSubsystem.cpp
Source/ProjectR/Divergence/PRDivergenceComponent.h
Source/ProjectR/Divergence/PRDivergenceComponent.cpp
Source/ProjectR/UI/PRDivergenceCacheWidget.h
Source/ProjectR/UI/PRDivergenceCacheWidget.cpp
Source/ProjectR/Divergence/Tests/PRDivergenceFoundationTests.cpp
Source/ProjectR/Divergence/Tests/PRDivergenceLifecycleTests.cpp
Source/ProjectR/Divergence/Tests/PRDivergenceAssetTests.cpp
Plugins/ProjectRAuthoringTools/Source/ProjectRAuthoringTools/Public/PRDivergenceAuthoringToolset.h
Plugins/ProjectRAuthoringTools/Source/ProjectRAuthoringTools/Private/PRDivergenceAuthoringToolset.cpp
Plugins/ProjectRAuthoringTools/Source/ProjectRAuthoringTools/Public/PRDivergenceAutomationToolset.h
Plugins/ProjectRAuthoringTools/Source/ProjectRAuthoringTools/Private/PRDivergenceAutomationToolset.cpp
```

Modify only:

```text
Plugins/ProjectRAuthoringTools/Source/ProjectRAuthoringTools/Private/ProjectRAuthoringToolsModule.cpp
.gitattributes
Docs/Versions/v0.3.4.md
Docs/DevelopmentLog.md
Docs/Architecture/DataAndInterfaceContracts.md
Docs/ArchitectureDecisions.md
Docs/KnownIssues.md (only if a real state change is found)
Docs/Workflow/UnrealMCPAuthoring.md (only if a new Toolset fact is found)
```

Create exactly:

```text
/Game/ProjectR/Data/Divergence/DA_DivergenceCache
/Game/ProjectR/UI/Companion/WBP_DivergenceCache
```

No existing UE Package may be modified. Existing Companion Registry, Dialogue Registry/DataAssets/Widget, ReverseBurn DataAsset, `L_CombatGym` and `L_BossGym` are read-only and must not be saved.

`DA_DivergenceCache` contains the fixed thresholds, 30-second timeout, revive fractions, Widget Class, three canonical Companion presentations and the nine exact Deltas. `WBP_DivergenceCache` subclasses `UPRDivergenceCacheWidget` and only renders prompt/options/state.

Use official MCP capabilities first. If they cannot create the fixed nested DataAsset or named Widget Tree, use a separate fixed no-argument `PRDivergenceAuthoringToolset` that can only Create/Repair/Validate/Save the two manifest Packages. `PRDivergenceAutomationToolset` exposes only `RunPIEDivergenceSmoke()` and `RunPIEDivergenceHumanPreview()`.

If the two Packages total no more than 1 MiB, add exact `.gitattributes` ordinary-Git exceptions for their two `.uasset` files. If larger, stop for an LFS storage decision.

# MCP Operation Manifest

1. Confirm a unique Editor, correct project, PIE=false and Dirty=0; audit Toolset schemas.
2. BuildEditor, restart Editor, and confirm both Create paths are absent and editable.
3. Create/configure the two Packages serially; read back every threshold, fraction, Delta, line and Widget Class before saving.
4. Compile WBP with warnings-as-errors and run Data Validation.
5. Save only the two manifest Packages. Any unexpected Dirty Package stops the run.
6. Restart normally and re-read both assets and all frozen references.
7. Run fixed PIE smoke and human preview; StopPIE and verify Widget, Actor, Timer, Delegate, ASC, Effect/Spec, test slot and Dirty cleanup.

Save All, Resave All, Fix Redirectors, package deletion/move/rename/copy, direct `.uasset/.umap` I/O and map saving are forbidden.

# TDD、验证与完成门

RED must prove missing divergence types, eligibility, three choices, assets or PIE behavior. GREEN must be the minimum implementation; no relaxed or skipped assertion.

`ProjectR.Divergence` covers schema/value-only fields; Trust 49/50 and Overload 79/80; player-only DeathEvent; duplicate and once-per-run behavior across travel; three outcomes; nine Deltas; exactly-one Save; Dialogue context; ReverseBurn/Support non-revive; and all cancellation/cleanup paths.

Run BuildEditor; ProjectR.Divergence; Input, GAS, Combat, Ability, Save, Debug, PlayerSkill, Enemy, Boss, Companion, QTE, Dialogue and CombatHUD regressions using their actual discovered counts; asset validation; compile; exact save; restart reload; and fixed PIE. PIE must use isolated test storage and pass `NoUserSaveTouched` and `TestSlotsCleaned`.

Required: `GitScope`, `TDDRed`, `BuildEditor`, `DivergenceSchema`, `EligibilityBoundaries`, `OncePerRun`, `DialogueValueConsumption`, `RescueRevive25`, `LeaveRemainsDead`, `ChallengeRevive10`, `RelationshipDelta`, `SingleSaveRequest`, `NoQTEAutoRevive`, `TravelPersistence`, `LifecycleCleanup`, `AssetsCreated`, `WidgetCompiled`, `ExactPackagesSaved`, `DirtyPackagesZero`, `RestartReload`, `FixedPIEDivergenceSmoke`, all applicable historical regressions, `NoGameplayTagsAdded`, `NoInputChanges`, `SaveSchema2Unchanged`, `NoMapsSaved`, `NoUserSaveTouched`, `HumanDivergenceClarity`, `FutureCompatibilityReview`.

Optional `NOT_RUN` remains valid for PhysicalGamepad, NetworkPIEReplication, Package and GC.

After every objective gate passes, Codex prepares one fixed preview. The user only confirms that the three voices, the three options and the E/R/F prompts are clear, replying `PASS` or “Primary + option + issue”.

# 回滚与 Future Compatibility Review

Stop if implementation needs Combat, AttributeSet, QTE, Companion support, Dialogue queue, Boss, Input, GameplayTag, Config, Save Schema, map, Room/Reward, PlayerProfile, account/graveyard or skill-tree changes; or if duplicate Revive/Delta/Save, compilation, restart, Dirty or regression failures occur.

Rollback only reverses v0.3.4’s exact source, tool, documentation and two-package diff. Deleting saved Packages needs explicit approval. Never hard-reset, clean, Save All or bulk-delete.

- v0.4.0 consumes only `FPRDivergenceResult` values for PlayerProfile and may replace the prototype run-boundary signal with formal RunState.
- v0.4.2 implements Room/Reward meaning for the published FutureDisposition without re-running Revive.
- v0.4.3 projects completed results into RunSummary/Graveyard and owns account deletion.
- v0.4.4 may add an unlock policy at the existing eligibility boundary without changing the P0 result, Combat or Save contracts.

# 唯一建议实现提交

After all required checks, human PASS, Future Compatibility Review and explicit user authorization:

```text
v0.3.4 Complete near-death rescue divergence system
```

No checkpoint versions or intermediate feature commits are allowed.

# 实施状态（Completed）

- 新增唯一 `UPRDivergenceSubsystem`、本地输入桥接组件与只读缓存 Widget。它只消费 Player 的正式 `Combat.Event.Death`、值型 Dialogue Result、Primary Sync 和 Save Operation；复活只经 `UPRCombatSubsystem`，关系只经既有 Companion Delta，实际关系变化才请求一次保存。没有修改 Combat、AttributeSet、QTE、Companion Support、Dialogue 队列、Boss、Input、GameplayTag、Config、Save Schema 或地图。
- 精确创建并保存 `/Game/ProjectR/Data/Divergence/DA_DivergenceCache` 与 `/Game/ProjectR/UI/Companion/WBP_DivergenceCache`。两项合计 35,631 bytes，低于 1 MiB，因此仅它们通过 `.gitattributes` 精确普通 Git 例外提交；没有新 LFS 对象、既有 Package 修改或地图保存。最终 Authoring 回读、Widget warnings-as-errors 编译、精确保存（`saved=0`、`mapsSaved=false`）和正常 Editor 重启均 PASS。
- TDD RED 记录缺少 Divergence 基础类型、DataAsset 与资格边界；对应 GREEN BuildEditor 通过，`ProjectR.Divergence` 3/3 验证固定枚举/阈值、九项 Delta、Trust 49/50 与 Overload 79/80。固定隔离 PIE smoke 返回 `rescueRevive25=true`、`leaveRemainsDead=true`、`challengeRevive10=true`、`singleSaveRequest=true`、`saveTouched=false` 与 `runtimeClean=true`。
- 新鲜独立进程历史回归均以退出码 0 完成：Input 3/3、GAS 4/4、Combat 5/5、Ability 6/6、Save 5/5、Debug 12/12、PlayerSkill 5/5、Enemy 11/11、Boss 3/3、Companion 7/7、QTE 5/5、Dialogue 5/5、CombatHUD 6/6。初始无 World 的根测试仅用于识别环境前提；最终每个需要 GameInstance 的套件均以正确的独立会话重新验证。
- 用户于 2026-07-25 完成 Axiom、Kindle、Null 的固定分歧卡清晰度预览并明确回复 `PASS`。最终报告为 `Saved/AutomationReports/v034-final-report-20260725/v034-final-None/result.json`，所有 required check PASS；PhysicalGamepad、NetworkPIEReplication、Package、GC 如实保持 optional `NOT_RUN`。
- Future Compatibility Review PASS：v0.4.0 只消费 `FPRDivergenceResult` 值建立画像；v0.4.2 只解释既有 FutureDisposition 的 Room/Reward 语义；v0.4.3 才把完成值投影到 RunSummary/Graveyard/Account；v0.4.4 只能在当前资格边界追加解锁策略。上述版本均不得重跑 Revive、接管关系/保存，或持久化运行时 UObject/Handle/Timer/Delegate。

# 正式提交与版本收尾

- 正式实施提交：`5703aaf374a1b0461498bf0afe1013bf5cc815fa`（`v0.3.4 Complete near-death rescue divergence system`）。
- 该提交已同时位于 `main` 与 `origin/main`；工作区中的后续文档修改不属于该实施提交。
- v0.3.4 已关闭。`CURRENT_VERSION.md` 保持指向 v0.3.4 并标记 `Completed`；本次不生成 v0.4.0 合同、不推进下一版本。
- PhysicalGamepad、NetworkPIEReplication、Package、GC 继续保持 optional `NOT_RUN`，未被收尾操作改写为 PASS。
