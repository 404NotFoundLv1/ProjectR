---
title: "ProjectR v0.0.0 仓库与 UE 基线"
subtitle: "首次 Git、Build、Editor、MCP 与 PIE 审计记录"
version: "v1.0"
date: "2026-07-10"
---

# 基线目标

把已存在的 UE5.8 模板工程整理为可审计的首次仓库边界。v0.0.0 不修改玩法代码、Config 或 UE Package，不创建未来版本类型。

# Git 启动审计

- 工作区：`E:/MyWork/ProjectR`
- 分支：`main`
- 初始 HEAD：不存在；没有提交。
- 远端：`origin = https://github.com/404NotFoundLv1/ProjectR.git`；本版本不访问、不提交、不推送。
- 清理前索引：1299 个路径，1297 个 `A`、2 个 `AM`。
- 清理前未跟踪文件：293 个；总状态记录 1592 个。
- 清理前暂存工作树约 4.91 GB，其中约 4.77 GB 是生成或本地状态。
- 清理前索引树：`e32a2786e2a870f622f5e715c6e502d2c179cea0`。
- 索引清理：`git rm -r -f --cached --ignore-unmatch .`；只改索引。
- 清理结果：移除 1299 个索引项，工作树缺失文件 0。
- 不执行 `git clean`、`git reset --hard`、`git gc`、commit 或 push。

# 受保护工程清单

| 范围 | 数量/状态 |
|---|---|
| `ProjectR.uproject` | EngineAssociation `5.8`；Runtime 模块 `ProjectR` |
| `Source` | 87 个文件 |
| Combat / SideScrolling / Platforming Variant Source | 42 / 26 / 8 个文件 |
| `Content` | 753 个 Package，140,920,074 字节 |
| `.uasset` / `.umap` | 749 / 4 |
| `Config` | 5 个文件 |
| 默认地图 | `/Game/ThirdPerson/Lvl_ThirdPerson` |
| Variant 地图 | `/Game/Variant_Combat/Lvl_Combat`、`/Game/Variant_Platforming/Lvl_Platforming`、`/Game/Variant_SideScrolling/Lvl_SideScrolling` |

清理前保护文件聚合 SHA-256 为 `D98B3BA6020416690AB7E2292AEE3BD22AE80E27CB5CFD5C50B618F0100BC9A4`。保护集合包含 `ProjectR.uproject`、`AGENTS.md`、`CURRENT_VERSION.md`、`.mcp.json`、`.vsconfig`、`Source/**`、`Content/**` 和 `Config/**`。

# 目录与存储策略

- 将磁盘目录 `docs` 经工作区内临时名修正为冻结合同要求的 `Docs`。
- 修正前后均为 22 份文档；归一化路径后的聚合 SHA-256 均为 `86913EAA74AD2BA94D733DB8FD0388EC07186CABEE5ADBDC3E037C13C558657F`。
- `.gitignore` 排除 Unreal 生成目录、IDE 状态、客户端本地配置与 `tmp`，但保留 `.mcp.json`、`.vsconfig`、工程、源码、资产、配置、文档和 BuildScripts。
- `.gitattributes` 仅让 `.uasset`、`.umap` 使用 Git LFS；不引入未来原始资产格式或锁定策略。
- 客户端目录 `.codex`、`.cursor`、`.gemini`、`.vscode` 保留在本机但不入库；仓库级 MCP 入口是 `.mcp.json`。

# UE5.8 与工具链

- Engine：`D:/Unreal Engine 5/UE_5.8`
- 版本：5.8.0，CL 55116800，`++UE5+Release-5.8`
- Editor Target：`ProjectREditor Win64 Development`
- Visual Studio：`D:/VisualStudio1`，18.5.2
- MSVC：14.50.35717；MSBuild：18.5
- 正式构建入口：`Engine/Build/BatchFiles/Build.bat ProjectREditor Win64 Development -Project=E:/MyWork/ProjectR/ProjectR.uproject -WaitMutex -FromMsBuild -architecture=x64`

# MCP Operation Manifest

- UE Package 写入目标：无。
- 允许的只读/验证工具：`list_toolsets`、ObjectTools 类搜索、Scene 当前关卡、EditorApp 选择/打开资产/PIE 状态/截图、Logs 日志读取。
- 允许的状态操作：仅 `StartPIE`、`StopPIE`。
- 禁止：资产创建、覆盖、移动、删除、保存、Blueprint 修改、Config 修改和 Resave。
- 当前连接：`127.0.0.1:8000` 由打开 `ProjectR.uproject` 的 PID 35820 监听。
- 身份证据：MCP 可反射 `/Script/ProjectR.*`；当前关卡 `/Game/ThirdPerson/Lvl_ThirdPerson`。
- 启动审计：19 个 Toolset；无选中 Actor/资产、无打开资产、PIE 未运行。

# 实际 Build 与 Editor 结果

- 第一次正式 Build 运行到 UHT 完成后退出 1；根因是 PID 35820 的 ProjectR Editor 正在运行 Live Coding。日志未出现 C++ 编译错误，结果为 `Failed (OtherCompilationError)`。
- 经用户明确授权，向 PID 35820 发送正常关闭窗口消息；Editor 正常退出，ProjectR Live Coding 进程结束，ProjectA PID 34880 保持运行。没有强杀、Save All 或资产写入。
- 用完全相同的命令重跑 Build，完成 48/48 actions，`Result: Succeeded`，退出码 0，总执行时间 69.89 秒。
- 实际工具链为 Visual Studio 14.50.35730、MSVC 14.50.35717、Windows SDK 10.0.22621.0。
- 输出 `UnrealEditor-ProjectR.dll`、`.lib` 和 Target metadata 均位于已忽略的 Binaries/Intermediate 路径。
- Build 后重新启动 ProjectR Editor PID 39504；该进程持有 `127.0.0.1:8000`，ProjectA 保持运行。
- 重启加载验证：19 个 Toolset 可用，当前关卡正确，`/Script/ProjectR.ProjectRCharacter` 可反射，无选中 Actor/资产或打开资产，PIE 初始为 false。

# 自动 PIE 结果

- 以非 Simulate、`PlayMode_InViewPort`、3 秒 warmup 启动默认 PIE。
- 多次验证中 `IsPIERunning` 在启动后为 true，`StopPIE` 后为 false。
- 三次独立的 PIE 日志增量比较均为新增 fatal/error/warning/ensure/assert 0 条；启动会话既有 45 条历史引擎/插件警告不计入本轮结果。
- `CaptureViewport` 的 Tool Schema 实际要求显式提供 `captureTransform` 和 `annotations`，虽然描述将其标为可选；补全参数后成功返回 `image/png`。
- 截图画面非空、非黑屏，可见默认 ThirdPerson 灰盒关卡与天空；截图未写入磁盘或 UE Package。
- 自动 PIE 冒烟：PASS。
- 人工操作验收：用户按 `Docs/Workflow/ManualOperationsRunbook.md` 第 6 节执行，移动 PASS、镜头 PASS、跳跃 PASS，未报告失败复现。

# Git 暂存结果

- 白名单索引：876 个路径。
- UE LFS：753 个路径，与 749 个 `.uasset` + 4 个 `.umap` 完全一致。
- 索引中的 Binaries、DerivedDataCache、Intermediate、Saved、`.vs`、`tmp`：0。
- 索引中的 `.codex`、`.cursor`、`.gemini`、`.vscode`、`.superpowers`：0。
- `Docs/`：23 个路径；小写 `docs/`：0。
- 本轮不创建 commit，因此 v0.0.0 和 KI-001 继续保持 In Progress/Open。

# 静态检查结果

- `git diff --check`：PASS，工作树与索引之间没有未暂存空白错误。
- `git diff --cached --check`：FAIL。仓库无 HEAD，因此命令检查全部首次导入文件，并报告官方模板 Source/Config、既有合同 Markdown hard-break 与 EOF 空行。
- `Source/Content/Config/ProjectR.uproject` 等 850 个保护文件聚合 SHA-256 与实施前相同，证明只读范围的空白问题不是本轮引入。
- 本版本不越权格式化 Source/Config 或重写全部既有文档；问题记录为 KI-010，v0.0.0 保持 In Progress。

# 兼容边界

- 工程名、模块名、Targets、默认地图、Redirect、GameplayTag 命名、Blueprint API、Save 字段和资产路径均不修改。
- 模板 Variant 原样保留，只记录为参考，不增加正式系统依赖。
- `BuildScripts/README.md` 只保留 v0.0.4 扩展入口，不实现未来构建系统。
- v0.0.1 可继续添加规范文档和 `Source/ProjectR/Core/PRDeveloperSettings.*`，无需重写本基线。
